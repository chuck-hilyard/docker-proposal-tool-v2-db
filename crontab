1 * * * * if [ -d /data/db ]; then mkdir /data/db/`date '+%Y-%m-%d-%H00'`; else :; fi
10 * * * * mongodump -u admin -p admin  -o /data/db/`date '+%Y-%m-%d-%H00'`
20 * * * * aws s3 sync /data/db/`date '+%Y-%m-%d-%H00'` s3://proposal-tool-v2-db-$(echo ${environment})-$(echo ${platform})
